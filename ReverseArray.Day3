void reverseArray(vector<int> &arr) {
        // code here
       int temp[arr.size()];
       for(int i=0;i<arr.size();i++){
           temp[i]=arr[arr.size()-1-i];
       }
            for(int i=0;i<arr.size();i++){
                arr[i]=temp[i];
            }  

    }
