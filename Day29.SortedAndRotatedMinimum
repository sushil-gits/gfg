int findMin(vector<int>& arr) {
        // complete the function here
        int min=INT_MAX;
        int n=arr.size();
        for(int i=0;i<n;i++){
            if(arr[i]<min)
            {
                min=arr[i];
            }
        }
        return min;
    }
